<?xml version="1.0" encoding="utf-8"?>
<!-- The launcher icon reuses the plugin glyph (see ICON_DRAWABLE in the
     manifest), so the app icon, the themed icon and the in-app mark stay
     in sync from a single drawable. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/nexus_glyph_tuya"
    android:inset="20%" />
